{
  "name": "quiz",
  "version": "1.0.0",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-share": "^5.2.2"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "@vitejs/plugin-react": "^4.3.0"
  },
  "scripts": {
    "dev": "cd no-quiz-app && npm run dev",
    "build": "cd no-quiz-app && npm run build",
    "preview": "cd no-quiz-app && npm run preview"
  }
}